# Server Settings
PORT=8000
SECRET_KEY=super-secret-enterprise-jwt-key-change-in-prod

# Database Connection (SQLite default for dev, PostgreSQL for production)
# DATABASE_URL=postgresql://user:password@localhost:5432/enterprise_contacts
DATABASE_URL=sqlite:///./enterprise_contacts.db
